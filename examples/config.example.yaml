# Gateway configuration for remote MCP servers
gateways:
  # Example: Connect to a remote MCP server
  - name: remote1
    url: http://localhost:9000/mcp
    prefix: remote1_

  # Example: Connect to another remote MCP server
  - name: remote2
    url: http://localhost:9001/mcp
    prefix: remote2_
